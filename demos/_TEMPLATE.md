# <One-line question or demo name>

Built on: `entries/<a>.md`, `datasets/<b>.md`

## What it shows

One sentence. For a reproduced demo: what the upstream demo claims to do.
For an experiment: the claim under test, stated so a result could refute it.

## Setup

What varied, what was held fixed, how many runs. `run.sh` reproduces it.

## Result

One figure (`figs/`), one table.

## What it means

Where the claim held, where it did not. What to run next.

## Hardware and wall clock

| | |
|---|---|
| Tested on | |
| Wall clock | |
